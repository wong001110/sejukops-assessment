import { NextResponse } from "next/server";
import { aiObservationListQuerySchema } from "@/domain/ai-observability/contracts";
import { listAIObservationsPaged } from "@/lib/observability/ai-observation-listing";
export const runtime="nodejs";
function errorCode(error:unknown):string|null{if(!error||typeof error!=="object")return null;const code=Reflect.get(error,"code");return typeof code==="string"?code:null;}
export async function GET(request:Request){try{const params=new URL(request.url).searchParams;const query=aiObservationListQuerySchema.parse({task:params.get("task")||undefined,status:params.get("status")||undefined,page:params.get("page")||undefined,pageSize:params.get("pageSize")||undefined});return NextResponse.json(await listAIObservationsPaged(query));}catch(error){const code=errorCode(error);if(code==="PERMISSION_DENIED"||code==="DEMO_SESSION_REQUIRED")return NextResponse.json({error:{code:"DIAGNOSTICS_PERMISSION_DENIED",message:"Technical diagnostics are available to Admin and Manager demo sessions."}},{status:403});if(code==="SUPABASE_SERVICE_ROLE_CONFIGURATION_MISSING")return NextResponse.json({error:{code:"DIAGNOSTICS_UNAVAILABLE",message:"Central AI observability requires the configured server data connection."}},{status:503});return NextResponse.json({error:{code:"DIAGNOSTICS_UNAVAILABLE",message:"AI observability is temporarily unavailable."}},{status:503});}}
